<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        @yield('contentheader_title', 'Trang chủ')
        <small>@yield('contentheader_description')</small>
    </h1>
    @yield('breadcrumb')
</section>